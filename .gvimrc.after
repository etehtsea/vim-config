set background=dark
colorscheme wombat

if has("gui_mac") || has("gui_macvim")
  set guifont=Monaco:h14
end

" disable toolbar
set guioptions-=T

set encoding=utf8

" for some reasons official option don't work
au VimEnter * :IndentGuidesEnable
let g:indent_guides_color_change_percent=3
